mutation ReviewCreate($productId: ID!) {
	createReview(
		data: {
			content: "content"
			email: "email"
			headline: "headline"
			name: "name"
			product: { connect: { id: $productId } }
			rating: 1
		}
	) {
		id
	}
}
